import React from "react";
import CheckoutButton from "../components/CheckoutButton";

const Profile = () => {
  return (
    <div>
      <CheckoutButton />
    </div>
  );
};

export default Profile;
